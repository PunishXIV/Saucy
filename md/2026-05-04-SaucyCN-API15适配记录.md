# 2026-05-04 SaucyCN API15 适配记录

## 背景

今天对 `E:\git\bubu\SaucyCN` 做了 Dalamud API 15 适配，并参考 `E:\git\SaucyCN` 里已经验证过的改动，把旧版 API 相关的工程配置和源码一起收敛到可编译、可加载的状态。

## 完成内容

### 1. 工程切换到 API15

- 将 `Saucy.csproj` 切换为 `Dalamud.NET.Sdk/15.0.0`
- 将 `Saucy.json` 里的 `DalamudApiLevel` 从 `14` 提升到 `15`
- 同步了参考仓库里已经验证过的 `packages.lock.json`

### 2. 补齐缺失依赖

- 目标仓库里原本缺少 `PunishLib` 子项目，导致解决方案无法正常还原
- 已将 `E:\git\SaucyCN\PunishLib` 补到 `E:\git\bubu\SaucyCN\PunishLib`
- 现在 `Saucy.sln` 可以正常还原和编译

### 3. 同步 API15 兼容源码

- 对照参考仓库同步了已验证的兼容改动
- 主要覆盖：
  - `Saucy.cs`
  - `CuffACur/CufModule.cs`
  - `OtherGames/AnyWayTheWindBlows.cs`
  - `OtherGames/SliceIsRight.cs`
  - `OutOnALimb/ECEmbedded/AtkReader.cs`
  - `OutOnALimb/LimbManager.cs`
  - `TripleTriad/TriadAutomater.cs`
  - `TripleTriad/TriadBuddy` 下的配置、窗口、本地化和读取器相关文件

### 4. 修复运行时崩溃

- 之前插件加载时会因为 `OnMessageDelegate` 找不到而在构造阶段崩溃
- 最终把 `OutOnALimb/LimbManager.cs` 的聊天事件订阅改成运行时反射绑定，避免直接依赖旧版委托类型
- 这一层修复后，插件构造可以正常完成

## 验证结果

已确认以下命令通过：

```powershell
dotnet build E:\git\bubu\SaucyCN\Saucy.sln -c Release
```

构建结果：

- `Saucy.dll` 生成成功
- `PunishLib.dll` 生成成功
- 仍有 1 个 warning：`CufModule.ShotFired` 未使用

## Release 输出

- `E:\git\bubu\SaucyCN\Saucy\bin\x64\Release\Saucy.dll`
- `E:\git\bubu\SaucyCN\PunishLib\PunishLib\bin\Release\PunishLib.dll`

## 备注

- 这次适配的关键点不是单纯改项目版本，而是把 API15 下已经失效的聊天事件绑定一起修掉
- 后续如果还有加载失败，优先检查的是 Triad 相关签名扫描，而不是工程版本本身
